import { TrashIcon } from '@/components/icons'
import { ButtonIcon } from '@/components/ui'

const ModuleDeleteModalButton = () => {
  return <ButtonIcon icon={<TrashIcon />} />
}

export default ModuleDeleteModalButton
